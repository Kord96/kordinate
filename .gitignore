# Ephemeral / cache
cache/
sessions/
history.jsonl
telemetry/
shell-snapshots/
file-history/
plans/
backups/
session-env/
mcp-needs-auth-cache.json

# Per-project tool results and memory (large, ephemeral)
/projects/

# Plugins (can be reinstalled)
plugins/

# Agent state is session-ephemeral
agent-state/

# Session worktrees
.worktrees/

# Secrets and credentials
.credentials.json

# Symlinks and copies (created by link.sh)
/CLAUDE.md
/settings.json
/keybindings.json
/.mcp.json
/agents
/agent-memory
/commands
/hooks
/profile

# Agent runtime state
kordinate/agents/*/deploys/

# CLAUDE.md copies (created by link.sh deploy, not tracked)
kordinate/agents/*/CLAUDE.md


# Legacy
__pycache__/
.merge-lock/
tasks/
mcp-needs-auth-cache.json

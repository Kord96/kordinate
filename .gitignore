# Ephemeral / cache
cache/
sessions/
history.jsonl
telemetry/
shell-snapshots/
file-history/
plans/
backups/
session-env/
mcp-needs-auth-cache.json

# Per-project tool results and memory (large, ephemeral)
/projects/

# Plugins (can be reinstalled)
plugins/

# Agent state is session-ephemeral
agent-state/

# Secrets and credentials
.credentials.json
.*-secret
.mcp.json

# Real config with IPs (generated from template)
config.yaml

# Profile — user content ignored, scaffolding tracked
profile/topology.yaml
profile/config.yaml
profile/overlays/*/
profile/dashboards/*/
profile/additions/*/
profile/projects/*/agent-memory/

# Agent runtime memory
agent-memory/

# Agent runtime state
agents/*/inbox.md
agents/*/deploys/
agents/*/knowledge/index.yaml
agents/*/knowledge/projects/

# Legacy
__pycache__/
.merge-lock/
tasks/
mcp-needs-auth-cache.json

# Memory Curator v2 Deployments — one per agent type.
# Replicas=1, strategy=Recreate (never run two curators simultaneously).
# Consumes memory.updates.<agent>, merges into shared PVC, updates CLAUDE.md.
# v2: Uses Anthropic Haiku for semantic deduplication.
#     Routes memory to memory/global/ or memory/projects/<project>/ based on scope.
#     Requires Secret 'anthropic-api' with key 'api-key'.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: curator-augur
  labels: { app: kord-curator, agent: augur }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: kord-curator, agent: augur }
  template:
    metadata:
      labels: { app: kord-curator, agent: augur }
    spec:
      serviceAccountName: kord
      containers:
        - name: curator
          image: localhost:30500/agent-factory:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /kord/agents/augur/memory/global /kord/agents/augur/memory/projects
              cd /kord/kordinate/lib/memory-curator && npm install --production 2>/dev/null
              exec node curator.js
          env:
            - { name: AGENT_NAME, value: augur }
            - { name: AGENT_PROJECT_DIR, value: /kord/agents/augur }
            - { name: PROJECTS_ROOT, value: /kord/projects }
            - { name: KAFKA_BROKERS, value: "kafka-kafka-bootstrap.dev.svc.cluster.local:9092" }
            - { name: KORDINATE_HOME, value: /kord/kordinate }
            - { name: HOME, value: /home/claude }
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-api
                  key: api-key
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
          volumeMounts:
            - { name: kord, mountPath: /kord }
      volumes:
        - name: kord
          persistentVolumeClaim: { claimName: kord }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: curator-charon
  labels: { app: kord-curator, agent: charon }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: kord-curator, agent: charon }
  template:
    metadata:
      labels: { app: kord-curator, agent: charon }
    spec:
      serviceAccountName: kord
      containers:
        - name: curator
          image: localhost:30500/agent-factory:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /kord/agents/charon/memory/global /kord/agents/charon/memory/projects
              cd /kord/kordinate/lib/memory-curator && npm install --production 2>/dev/null
              exec node curator.js
          env:
            - { name: AGENT_NAME, value: charon }
            - { name: AGENT_PROJECT_DIR, value: /kord/agents/charon }
            - { name: PROJECTS_ROOT, value: /kord/projects }
            - { name: KAFKA_BROKERS, value: "kafka-kafka-bootstrap.dev.svc.cluster.local:9092" }
            - { name: KORDINATE_HOME, value: /kord/kordinate }
            - { name: HOME, value: /home/claude }
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-api
                  key: api-key
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
          volumeMounts:
            - { name: kord, mountPath: /kord }
      volumes:
        - name: kord
          persistentVolumeClaim: { claimName: kord }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: curator-warden
  labels: { app: kord-curator, agent: warden }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: kord-curator, agent: warden }
  template:
    metadata:
      labels: { app: kord-curator, agent: warden }
    spec:
      serviceAccountName: kord
      containers:
        - name: curator
          image: localhost:30500/agent-factory:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /kord/agents/warden/memory/global /kord/agents/warden/memory/projects
              cd /kord/kordinate/lib/memory-curator && npm install --production 2>/dev/null
              exec node curator.js
          env:
            - { name: AGENT_NAME, value: warden }
            - { name: AGENT_PROJECT_DIR, value: /kord/agents/warden }
            - { name: PROJECTS_ROOT, value: /kord/projects }
            - { name: KAFKA_BROKERS, value: "kafka-kafka-bootstrap.dev.svc.cluster.local:9092" }
            - { name: KORDINATE_HOME, value: /kord/kordinate }
            - { name: HOME, value: /home/claude }
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-api
                  key: api-key
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
          volumeMounts:
            - { name: kord, mountPath: /kord }
      volumes:
        - name: kord
          persistentVolumeClaim: { claimName: kord }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: curator-sauron
  labels: { app: kord-curator, agent: sauron }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: kord-curator, agent: sauron }
  template:
    metadata:
      labels: { app: kord-curator, agent: sauron }
    spec:
      serviceAccountName: kord
      containers:
        - name: curator
          image: localhost:30500/agent-factory:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /kord/agents/sauron/memory/global /kord/agents/sauron/memory/projects
              cd /kord/kordinate/lib/memory-curator && npm install --production 2>/dev/null
              exec node curator.js
          env:
            - { name: AGENT_NAME, value: sauron }
            - { name: AGENT_PROJECT_DIR, value: /kord/agents/sauron }
            - { name: PROJECTS_ROOT, value: /kord/projects }
            - { name: KAFKA_BROKERS, value: "kafka-kafka-bootstrap.dev.svc.cluster.local:9092" }
            - { name: KORDINATE_HOME, value: /kord/kordinate }
            - { name: HOME, value: /home/claude }
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-api
                  key: api-key
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
          volumeMounts:
            - { name: kord, mountPath: /kord }
      volumes:
        - name: kord
          persistentVolumeClaim: { claimName: kord }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: curator-scribe
  labels: { app: kord-curator, agent: scribe }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: kord-curator, agent: scribe }
  template:
    metadata:
      labels: { app: kord-curator, agent: scribe }
    spec:
      serviceAccountName: kord
      containers:
        - name: curator
          image: localhost:30500/agent-factory:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /kord/agents/scribe/memory/global /kord/agents/scribe/memory/projects
              cd /kord/kordinate/lib/memory-curator && npm install --production 2>/dev/null
              exec node curator.js
          env:
            - { name: AGENT_NAME, value: scribe }
            - { name: AGENT_PROJECT_DIR, value: /kord/agents/scribe }
            - { name: PROJECTS_ROOT, value: /kord/projects }
            - { name: KAFKA_BROKERS, value: "kafka-kafka-bootstrap.dev.svc.cluster.local:9092" }
            - { name: KORDINATE_HOME, value: /kord/kordinate }
            - { name: HOME, value: /home/claude }
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-api
                  key: api-key
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
          volumeMounts:
            - { name: kord, mountPath: /kord }
      volumes:
        - name: kord
          persistentVolumeClaim: { claimName: kord }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: curator-alfred
  labels: { app: kord-curator, agent: alfred }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: kord-curator, agent: alfred }
  template:
    metadata:
      labels: { app: kord-curator, agent: alfred }
    spec:
      serviceAccountName: kord
      containers:
        - name: curator
          image: localhost:30500/agent-factory:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /kord/agents/alfred/memory/global /kord/agents/alfred/memory/projects
              cd /kord/kordinate/lib/memory-curator && npm install --production 2>/dev/null
              exec node curator.js
          env:
            - { name: AGENT_NAME, value: alfred }
            - { name: AGENT_PROJECT_DIR, value: /kord/agents/alfred }
            - { name: PROJECTS_ROOT, value: /kord/projects }
            - { name: KAFKA_BROKERS, value: "kafka-kafka-bootstrap.dev.svc.cluster.local:9092" }
            - { name: KORDINATE_HOME, value: /kord/kordinate }
            - { name: HOME, value: /home/claude }
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-api
                  key: api-key
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
          volumeMounts:
            - { name: kord, mountPath: /kord }
      volumes:
        - name: kord
          persistentVolumeClaim: { claimName: kord }

# Generated from agents/charon/skills/platform/agent-spec.yaml
# One request topic and one ScaledObject per agent.

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-generic-opus
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-generic-opus
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: generic-opus
        topic: generic-opus
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-augur-opus
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-augur-opus
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: augur-opus
        topic: augur-opus
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-augur-gemini-pro
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-augur-gemini-pro
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: augur-gemini-pro
        topic: augur-gemini-pro
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-augur-deepseek-reasoner
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-augur-deepseek-reasoner
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: augur-deepseek-reasoner
        topic: augur-deepseek-reasoner
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-augur-glm5
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-augur-glm5
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: augur-glm5
        topic: augur-glm5
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-augur-gpt54
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 1
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-augur-gpt54
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: augur-gpt54
        topic: augur-gpt54
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-charon-gpt53-codex
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-charon-gpt53-codex
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: charon-gpt53-codex
        topic: charon-gpt53-codex
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-warden-haiku
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-warden-haiku
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: warden-haiku
        topic: warden-haiku
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-sauron-sonnet
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 3
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-sauron-sonnet
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: sauron-sonnet
        topic: sauron-sonnet
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-alfred-deepseek-chat
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 1
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-alfred-deepseek-chat
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: alfred-deepseek-chat
        topic: alfred-deepseek-chat
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-alfred-gpt-oss-20b
spec:
  pollingInterval: 30
  minReplicaCount: 1
  maxReplicaCount: 1
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: true
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 300
  scaleTargetRef:
    name: agent-alfred-gpt-oss-20b
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka-kafka-bootstrap.dev.svc.cluster.local:9092
        consumerGroup: alfred-gpt-oss-20b
        topic: alfred-gpt-oss-20b
        lagThreshold: "1"
        activationLagThreshold: "0"
        offsetResetPolicy: earliest

version: 2

# Source-of-truth agent platform spec.
#
# `name` is the deployed agent identity.
# `flavor` selects the seeded specialist overlay:
# - `augur`, `charon`, `alfred`, `sauron`
# - `generic` for plain runtime/backend behavior with no seeded overlay
#
# `runtime.command` is the in-container executable. This should resolve to the
# shared daemon package baked into the image.
# `runtime.daemon` describes the shared daemon runtime/backend selection.

agents:
  - name: generic-opus
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: opus
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: generic-opus
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-opus
        state_dir: /kord/agents/generic-opus
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-opus
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: opus
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: augur-opus
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-opus
        state_dir: /kord/agents/augur-opus
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: generic-gemini-31-pro
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: gemini
        model: gemini-3.1-pro-preview
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: generic-gemini-31-pro
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-gemini-31-pro
        state_dir: /kord/agents/generic-gemini-31-pro
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: generic-gemini-25-pro
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: gemini
        model: gemini-2.5-pro
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: generic-gemini-25-pro
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-gemini-25-pro
        state_dir: /kord/agents/generic-gemini-25-pro
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: generic-gemini-25-flash
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: gemini
        model: gemini-2.5-flash
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: generic-gemini-25-flash
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-gemini-25-flash
        state_dir: /kord/agents/generic-gemini-25-flash
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: generic-deepseek-reasoner
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: deepseek
        model: deepseek-reasoner
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: BACKEND_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: generic-deepseek-reasoner
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-deepseek-reasoner
        state_dir: /kord/agents/generic-deepseek-reasoner
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: generic-glm5
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: fireworks
        model: accounts/fireworks/models/glm-5
        backend:
          name: fireworks
          base_url: https://api.fireworks.ai/inference/v1
        secret:
          env: BACKEND_API_KEY
          name: fireworks-api
          key: api-key
      kafka:
        request_topic: generic-glm5
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-glm5
        state_dir: /kord/agents/generic-glm5
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: generic-gpt54
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: codex-sdk
        provider: openai
        model: gpt-5.4
        backend:
          name: openai
          base_url: https://api.openai.com/v1
        skip_git_repo_check: true
        sandbox_mode: workspace-write
        secret:
          env: BACKEND_API_KEY
          name: codex-api
          key: api-key
      kafka:
        request_topic: generic-gpt54
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/generic-gpt54
        state_dir: /kord/agents/generic-gpt54
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gemini-31-pro
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: gemini
        model: gemini-3.1-pro-preview
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: augur-gemini-31-pro
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-gemini-31-pro
        state_dir: /kord/agents/augur-gemini-31-pro
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gemini-25-pro
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: gemini
        model: gemini-2.5-pro
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: augur-gemini-25-pro
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-gemini-25-pro
        state_dir: /kord/agents/augur-gemini-25-pro
    creation:
      memory_bundle: analyze-selective-v1
      runtime_bundle: analyze-selective-v1
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gemini-25-flash
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: gemini-sdk
        provider: gemini
        model: gemini-2.5-flash
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: augur-gemini-25-flash
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-gemini-25-flash
        state_dir: /kord/agents/augur-gemini-25-flash
    creation:
      memory_bundle: analyze-selective-v1
      runtime_bundle: analyze-selective-v1
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-deepseek-reasoner
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: deepseek
        model: deepseek-reasoner
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: BACKEND_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: augur-deepseek-reasoner
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-deepseek-reasoner
        state_dir: /kord/agents/augur-deepseek-reasoner
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-glm5
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: fireworks
        model: accounts/fireworks/models/glm-5
        backend:
          name: fireworks
          base_url: https://api.fireworks.ai/inference/v1
        secret:
          env: BACKEND_API_KEY
          name: fireworks-api
          key: api-key
      kafka:
        request_topic: augur-glm5
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-glm5
        state_dir: /kord/agents/augur-glm5
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gpt54
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: codex-sdk
        provider: openai
        model: gpt-5.4
        backend:
          name: openai
          base_url: https://api.openai.com/v1
        skip_git_repo_check: true
        sandbox_mode: workspace-write
        secret:
          env: BACKEND_API_KEY
          name: codex-api
          key: api-key
      kafka:
        request_topic: augur-gpt54
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/augur-gpt54
        state_dir: /kord/agents/augur-gpt54
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: charon-gpt53-codex
    flavor: charon
    image:
      base: agent-base
      customization: agent-charon
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: codex-sdk
        provider: openai
        model: gpt-5.3-codex
        backend:
          name: openai
          base_url: https://api.openai.com/v1
        skip_git_repo_check: true
        sandbox_mode: danger-full-access
        secret:
          env: BACKEND_API_KEY
          name: codex-api
          key: api-key
      kafka:
        request_topic: charon-gpt53-codex
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/charon-gpt53-codex
        state_dir: /kord/agents/charon-gpt53-codex
    creation:
      profile: charon-gpt53-codex
      memory_bundle: platform-core-v1
      skill_bundle: platform-ops-v1
      runtime_bundle: ops-default-v1
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: sauron-gpt53-codex
    flavor: sauron
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: codex-sdk
        provider: openai
        model: gpt-5.3-codex
        backend:
          name: openai
          base_url: https://api.openai.com/v1
        skip_git_repo_check: true
        sandbox_mode: workspace-write
        secret:
          env: BACKEND_API_KEY
          name: codex-api
          key: api-key
      kafka:
        request_topic: sauron-gpt53-codex
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/sauron-gpt53-codex
        state_dir: /kord/agents/sauron-gpt53-codex
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: alfred-deepseek-chat
    flavor: alfred
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: deepseek
        model: deepseek-chat
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: DEEPSEEK_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: alfred-deepseek-chat
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/alfred-deepseek-chat
        state_dir: /kord/agents/alfred-deepseek-chat
    creation:
      profile: alfred-deepseek-chat
      memory_bundle: operate-direct-v1
      skill_bundle: get-store-core-v1
      runtime_bundle: direct-action-v1
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: alfred-gpt-oss-20b
    flavor: alfred
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: simple-harness
        provider: fireworks
        model: fireworks/gpt-oss-20b
        backend:
          name: fireworks
          base_url: https://api.fireworks.ai/inference/v1
        secret:
          env: BACKEND_API_KEY
          name: fireworks-api
          key: api-key
      kafka:
        request_topic: alfred-gpt-oss-20b
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /kord/agents/alfred-gpt-oss-20b
        state_dir: /kord/agents/alfred-gpt-oss-20b
    creation:
      profile: alfred-gpt-oss-20b
      memory_bundle: operate-direct-v1
      skill_bundle: get-store-core-v1
      runtime_bundle: direct-action-v1
    deploy:
      replicas:
        min: 1
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

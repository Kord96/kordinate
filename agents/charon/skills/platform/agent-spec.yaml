version: 2

# Source-of-truth agent platform spec.
#
# `name` is the deployed agent identity.
# `flavor` selects the seeded specialist overlay:
# - `augur`, `charon`, `alfred`, `sauron`, `warden`
# - `generic` for plain runtime/backend behavior with no seeded overlay
#
# `runtime.command` is the in-container executable. This should resolve to the
# shared daemon package baked into the image.
# `runtime.daemon` describes the shared daemon runtime/backend selection.

agents:
  - name: augur-opus
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-sdk
        provider: anthropic
        model: opus
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: augur-opus
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        project_dir: /runtime/augur-opus
        state_dir: /kord/augur-opus
    deploy:
      replicas:
        min: 0
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-pro
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: gemini
        model: gemini-3.1-pro-preview
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: augur-pro
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        project_dir: /runtime/augur-pro
        state_dir: /kord/augur-pro
    deploy:
      replicas:
        min: 0
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-reasoner
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: deepseek
        model: deepseek-reasoner
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: BACKEND_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: augur-reasoner
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        project_dir: /runtime/augur-reasoner
        state_dir: /kord/augur-reasoner
    deploy:
      replicas:
        min: 0
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-glm5
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: fireworks
        model: accounts/fireworks/models/glm-5
        backend:
          name: fireworks
          base_url: https://api.fireworks.ai/inference/v1
        secret:
          env: BACKEND_API_KEY
          name: fireworks-api
          key: api-key
      kafka:
        request_topic: augur-glm5
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        project_dir: /runtime/augur-glm5
        state_dir: /kord/augur-glm5
    deploy:
      replicas:
        min: 0
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gpt54
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: codex-sdk
        provider: openai
        model: gpt-5.4
        backend:
          name: openai
          base_url: https://api.openai.com/v1
        skip_git_repo_check: true
        secret:
          env: BACKEND_API_KEY
          name: codex-api
          key: api-key
      kafka:
        request_topic: augur-gpt54
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        project_dir: /runtime/augur-gpt54
        state_dir: /kord/augur-gpt54
    deploy:
      replicas:
        min: 0
        max: 1
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: charon
    flavor: charon
    image:
      base: agent-base
      customization: agent-charon
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-sdk
        provider: anthropic
        model: sonnet
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: agent.charon
        reply_required: true
        request_schema:
          required: [prompt, reply_to]
          optional: [timeout_ms, reflect]
        response_schema:
          required: [status, output]
          optional: [reflection, errors]
      state:
        project_dir: /runtime/charon
        state_dir: /kord/charon
    deploy:
      replicas:
        min: 0
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: warden
    flavor: warden
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-sdk
        provider: anthropic
        model: haiku
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: agent.warden
        reply_required: true
        request_schema:
          required: [prompt, reply_to]
          optional: [timeout_ms, reflect]
        response_schema:
          required: [status, output]
          optional: [reflection, errors]
      state:
        project_dir: /runtime/warden
        state_dir: /kord/warden
    deploy:
      replicas:
        min: 0
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: sauron
    flavor: sauron
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-sdk
        provider: anthropic
        model: sonnet
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: agent.sauron
        reply_required: true
        request_schema:
          required: [prompt, reply_to]
          optional: [timeout_ms, reflect]
        response_schema:
          required: [status, output]
          optional: [reflection, errors]
      state:
        project_dir: /runtime/sauron
        state_dir: /kord/sauron
    deploy:
      replicas:
        min: 0
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: alfred
    flavor: alfred
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: deepseek
        model: deepseek-chat
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: DEEPSEEK_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: agent.alfred
        reply_required: true
        request_schema:
          required: [prompt, reply_to]
          optional: [timeout_ms, reflect]
        response_schema:
          required: [status, output]
          optional: [reflection, errors]
      state:
        project_dir: /runtime/alfred
        state_dir: /kord/alfred
    deploy:
      replicas:
        min: 0
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

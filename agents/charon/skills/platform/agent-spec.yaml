version: 2

# Source-of-truth agent platform spec.
#
# `name` is the deployed agent identity.
# `flavor` selects the seeded specialist overlay:
# - `augur`, `charon`, `alfred`, `sauron`, `warden`
# - `generic` for plain runtime/backend behavior with no seeded overlay
#
# `runtime.command` is the in-container executable. This should resolve to the
# shared daemon package baked into the image.
# `runtime.daemon` describes the shared daemon runtime/backend selection.

agents:
  - name: generic-opus
    flavor: generic
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: opus
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: generic-opus
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/generic-opus
        state_dir: /kord/generic-opus
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-opus
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: opus
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: augur-opus
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/augur-opus
        state_dir: /kord/augur-opus
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gemini-pro
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: gemini
        model: gemini-3.1-pro-preview
        backend:
          name: gemini
          base_url: https://generativelanguage.googleapis.com/v1beta/openai
        secret:
          env: BACKEND_API_KEY
          name: gemini-api
          key: api-key
      kafka:
        request_topic: augur-gemini-pro
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/augur-gemini-pro
        state_dir: /kord/augur-gemini-pro
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-deepseek-reasoner
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: deepseek
        model: deepseek-reasoner
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: BACKEND_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: augur-deepseek-reasoner
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/augur-deepseek-reasoner
        state_dir: /kord/augur-deepseek-reasoner
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-glm5
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: fireworks
        model: accounts/fireworks/models/glm-5
        backend:
          name: fireworks
          base_url: https://api.fireworks.ai/inference/v1
        secret:
          env: BACKEND_API_KEY
          name: fireworks-api
          key: api-key
      kafka:
        request_topic: augur-glm5
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/augur-glm5
        state_dir: /kord/augur-glm5
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: augur-gpt54
    flavor: augur
    image:
      base: agent-base
      customization: agent-augur
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: codex-sdk
        provider: openai
        model: gpt-5.4
        backend:
          name: openai
          base_url: https://api.openai.com/v1
        skip_git_repo_check: true
        secret:
          env: BACKEND_API_KEY
          name: codex-api
          key: api-key
      kafka:
        request_topic: augur-gpt54
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/augur-gpt54
        state_dir: /kord/augur-gpt54
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: charon-sonnet
    flavor: charon
    image:
      base: agent-base
      customization: agent-charon
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: sonnet
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: charon-sonnet
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/charon-sonnet
        state_dir: /kord/charon-sonnet
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: warden-haiku
    flavor: warden
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: haiku
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: warden-haiku
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/warden-haiku
        state_dir: /kord/warden-haiku
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: sauron-sonnet
    flavor: sauron
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: claude-agent-sdk
        provider: anthropic
        model: sonnet
        backend:
          name: anthropic
        secret:
          env: ANTHROPIC_API_KEY
          name: anthropic-api
          key: api-key
      kafka:
        request_topic: sauron-sonnet
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/sauron-sonnet
        state_dir: /kord/sauron-sonnet
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: alfred-deepseek-chat
    flavor: alfred
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: deepseek
        model: deepseek-chat
        backend:
          name: deepseek
          base_url: https://api.deepseek.com/v1
        secret:
          env: DEEPSEEK_API_KEY
          name: deepseek-api
          key: api-key
      kafka:
        request_topic: alfred-deepseek-chat
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/alfred-deepseek-chat
        state_dir: /kord/alfred-deepseek-chat
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

  - name: alfred-gpt-oss-20b
    flavor: alfred
    image:
      base: agent-base
      customization: none
    runtime:
      command: ["klaude-daemon"]
      daemon:
        kind: openclaude-harness
        provider: fireworks
        model: fireworks/gpt-oss-20b
        backend:
          name: fireworks
          base_url: https://api.fireworks.ai/inference/v1
        secret:
          env: BACKEND_API_KEY
          name: fireworks-api
          key: api-key
      kafka:
        request_topic: alfred-gpt-oss-20b
        reply_required: true
        request_schema:
          required: [type, sender, correlation_id, prompt]
          optional: [working_dir, timeout_ms, reflect, reflection_prompt, agent_params]
        response_schema:
          required: [type, sender, correlation_id, status, output]
          optional: [reflection, errors, metadata]
      state:
        agent_home_dir: /runtime/alfred-gpt-oss-20b
        state_dir: /kord/alfred-gpt-oss-20b
    deploy:
      replicas:
        min: 1
        max: 3
        cooldown: 300
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          memory: 4Gi

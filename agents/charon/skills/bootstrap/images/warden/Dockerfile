ARG REGISTRY=local
FROM ${REGISTRY}/agent-base:latest

LABEL org.opencontainers.image.title="agent-warden"
LABEL org.opencontainers.image.description="Warden specialization layered on the shared agent-base runtime"

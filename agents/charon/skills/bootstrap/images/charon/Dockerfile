ARG REGISTRY=local
ARG BASE_IMAGE=${REGISTRY}/agent-base:latest
FROM ${BASE_IMAGE}

LABEL org.opencontainers.image.title="agent-charon"
LABEL org.opencontainers.image.description="Charon specialization layered on the shared agent-base runtime"

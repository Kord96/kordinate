ARG REGISTRY=local
ARG BASE_IMAGE=${REGISTRY}/agent-base:latest
FROM ${BASE_IMAGE}

LABEL org.opencontainers.image.title="agent-alfred"
LABEL org.opencontainers.image.description="Alfred specialization layered on the shared agent-base runtime"

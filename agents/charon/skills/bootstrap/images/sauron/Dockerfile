ARG REGISTRY=local
FROM ${REGISTRY}/agent-base:latest

LABEL org.opencontainers.image.title="agent-sauron"
LABEL org.opencontainers.image.description="Sauron specialization layered on the shared agent-base runtime"

##
## Gateway Alloy — central per-cluster collector
## Scrapes app metrics, kubelet/cAdvisor, KSM, Kafka JMX.
## Tails pod logs via Kubernetes API.
## Writes metrics to local Prometheus, logs to local Loki.
##
apiVersion: v1
kind: ServiceAccount
metadata:
  name: alloy
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: alloy
rules:
  - apiGroups: [""]
    resources:
      - nodes
      - nodes/proxy
      - nodes/metrics
      - pods
      - pods/log
      - services
      - endpoints
    verbs: ["get", "list", "watch"]
  - nonResourceURLs: ["/metrics", "/metrics/cadvisor"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: alloy
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: alloy
subjects:
  - kind: ServiceAccount
    name: alloy
    namespace: monitor
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alloy
  labels:
    app: monitoring
    component: alloy
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: monitoring
      component: alloy
  template:
    metadata:
      labels:
        app: monitoring
        component: alloy
    spec:
      serviceAccountName: alloy
      shareProcessNamespace: true
      containers:
        - name: alloy
          image: grafana/alloy:v1.5.1
          args:
            - run
            - /etc/alloy/config.alloy
            - --stability.level=generally-available
          env:
            - name: CLUSTER_NAME
              value: MUST_BE_SET_BY_OVERLAY
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              memory: 1Gi
          volumeMounts:
            - name: alloy-config
              mountPath: /etc/alloy
        - name: config-reloader
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - |
              HASH=$(md5sum /etc/alloy/config.alloy | cut -d' ' -f1)
              while true; do
                sleep 30
                NEW=$(md5sum /etc/alloy/config.alloy | cut -d' ' -f1)
                if [ "$NEW" != "$HASH" ]; then
                  PID=$(pgrep -f 'alloy.*run' | head -1)
                  [ -n "$PID" ] && kill -HUP "$PID" && echo "Alloy config reloaded (SIGHUP $PID)" || echo "Reload failed"
                  HASH=$NEW
                fi
              done
          volumeMounts:
            - name: alloy-config
              mountPath: /etc/alloy
              readOnly: true
          resources:
            requests:
              cpu: 5m
              memory: 8Mi
            limits:
              memory: 16Mi
      volumes:
        - name: alloy-config
          configMap:
            name: alloy-config

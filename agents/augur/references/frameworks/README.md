# Framework References

Each file in this directory is the canonical Augur reference for one framework.

Reference files are Markdown with frontmatter. The frontmatter carries the
machine-readable `signatures` used by detector and skill tooling, while the body
contains the human-readable explanation.

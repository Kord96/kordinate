# Detector Scripts

`detectors/scripts/` contains stable CLI entrypoints for detector-side helper
scripts.

The implementation lives under `detectors/utils/`. These wrappers provide a
predictable on-disk surface for other Augur scripts that invoke detector
helpers by path.

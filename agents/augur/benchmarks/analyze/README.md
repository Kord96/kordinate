# Analyze Benchmarks

This tree holds `/analyze` evaluation inputs and retained outputs.

- top-level files define datasets, repo sets, scoring plans, and comparison rubrics
- `ablations/` contains per-repo benchmark definitions and their retained run corpora

Use `../scripts/` to execute or aggregate benchmark runs.

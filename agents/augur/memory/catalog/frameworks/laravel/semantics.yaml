name: laravel
kind: framework
language: php
framework_kind: full-stack
summary: Full-stack PHP framework with expressive routing, ORM, middleware, and service container
status: primary
scope: backend
relationships:
  implements:
    - rest
  supports:
    - dependency-injection
    - input-validation
  uses:
    - server-route-registration
  related_to:
    - layered
traits:
  api_surface: true
  orm_native: true
  validation_native: true
  dependency_injection_native: true
common_concepts:
  - active-record
common_failure_modes:
  - fat-controllers
  - hidden-service-container-dependencies
  - model-leakage

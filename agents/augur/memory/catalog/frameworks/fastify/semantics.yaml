name: fastify
kind: framework
language: typescript
framework_kind: api-server
summary: High-performance Node.js web framework with schema-aware routes and plugin-driven composition
status: primary
scope: backend
relationships:
  implements:
    - rest
  supports:
    - input-validation
  uses:
    - server-route-registration
traits:
  api_surface: true
  validation_native: true
  plugin_native: true
common_concepts: []
common_failure_modes:
  - plugin-registration-sprawl
  - schema-drift
  - business-logic-in-routes

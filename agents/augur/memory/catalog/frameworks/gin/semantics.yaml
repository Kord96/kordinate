name: gin
kind: framework
language: go
framework_kind: api-server
summary: Go web framework with explicit routing and middleware around HTTP handlers
status: primary
scope: backend
relationships:
  implements:
    - rest
  uses:
    - server-route-registration
traits:
  api_surface: true
  middleware_native: true
common_concepts: []
common_failure_modes:
  - handler-bloat
  - middleware-ordering-surprises
  - global-engine-coupling

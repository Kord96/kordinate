---
description: Ruby API framework centered on declarative endpoint DSLs and resource blocks
---
# Grape

Grape is a Ruby API framework centered on declarative endpoint DSLs and resource blocks.

## Recognition
Use the detector package under `detectors/facts/frameworks/grape/` as the deterministic source of truth for framework evidence.

## Architectural implications
- framework scope: `backend`
- framework kind: `api-server`
- framework-native traits and relationships are defined in `semantics.yaml`

## Common failure modes
- endpoint-dsl-sprawl
- resource-block-bloat
- validation-logic-fragmentation

name: nestjs
kind: framework
language: typescript
framework_kind: api-server
summary: Opinionated TypeScript backend framework with decorators, modules, and dependency injection
status: primary
scope: backend
relationships:
  implements:
    - rest
  supports:
    - dependency-injection
    - input-validation
  uses:
    - server-route-registration
  related_to:
    - layered
traits:
  api_surface: true
  dependency_injection_native: true
  validation_native: true
  module_system_native: true
common_concepts: []
common_failure_modes:
  - decorator-heavy-indirection
  - service-layer-bloat
  - hidden-runtime-wiring

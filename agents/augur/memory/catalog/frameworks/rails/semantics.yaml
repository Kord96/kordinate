name: rails
kind: framework
language: ruby
framework_kind: full-stack
summary: Full-stack Ruby framework with conventions for routing, models, controllers, and views
status: primary
scope: backend
relationships:
  implements:
    - rest
  supports:
    - input-validation
  uses:
    - server-route-registration
  related_to:
    - layered
traits:
  api_surface: true
  orm_native: true
  validation_native: true
common_concepts:
  - active-record
common_failure_modes:
  - fat-models
  - callback-heavy-control-flow
  - implicit-convention-coupling

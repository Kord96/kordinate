name: spring
kind: framework
language: java
framework_kind: full-stack
summary: Java framework ecosystem with annotation-driven controllers, dependency injection, and broad infrastructure integration
status: primary
scope: backend
relationships:
  implements:
    - rest
  supports:
    - dependency-injection
    - input-validation
  uses:
    - server-route-registration
  related_to:
    - layered
traits:
  api_surface: true
  dependency_injection_native: true
  validation_native: true
common_concepts: []
common_failure_modes:
  - annotation-heavy-indirection
  - service-layer-bloat
  - hidden-runtime-magic

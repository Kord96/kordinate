name: starlette
kind: framework
language: python
framework_kind: api-server
summary: ASGI toolkit for Python services with explicit routes, middleware, and async request handling
status: specialized
scope: backend
relationships:
  implements:
    - rest
  uses:
    - server-route-registration
traits:
  api_surface: true
  async_native: true
  middleware_native: true
common_concepts: []
common_failure_modes:
  - mixed-sync-async
  - middleware-ordering-surprises
  - hand-rolled-validation

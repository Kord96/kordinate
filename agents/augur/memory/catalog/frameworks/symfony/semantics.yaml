name: symfony
kind: framework
language: php
framework_kind: full-stack
summary: PHP framework with explicit services, HTTP kernel, and attribute-driven routing
status: primary
scope: backend
relationships:
  implements:
    - rest
  supports:
    - dependency-injection
    - input-validation
  uses:
    - server-route-registration
  related_to:
    - layered
traits:
  api_surface: true
  dependency_injection_native: true
  validation_native: true
common_concepts: []
common_failure_modes:
  - service-graph-indirection
  - annotation-or-attribute-sprawl
  - config-fragmentation

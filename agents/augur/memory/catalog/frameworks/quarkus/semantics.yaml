name: quarkus
kind: framework
language: java
framework_kind: api-server
summary: Java framework for cloud-native services with JAX-RS routes and build-time optimization
status: specialized
scope: backend
relationships:
  implements:
    - rest
  supports:
    - dependency-injection
  uses:
    - server-route-registration
traits:
  api_surface: true
  dependency_injection_native: true
common_concepts: []
common_failure_modes:
  - annotation-heavy-indirection
  - build-time-magic
  - runtime-assumption-coupling

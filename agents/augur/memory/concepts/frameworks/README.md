# Framework Concepts

Each file in this directory is the canonical Augur reference for one framework.

Framework files are Markdown with frontmatter. The frontmatter carries signatures plus structured framework semantics used by detector and ontology tooling, while the body contains the human-readable explanation.

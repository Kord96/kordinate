questions:
  - q: "Is there an ordered list of handler objects where each handler can process a request or pass it to the next handler?"
    weight: 3
    signals: ["handler chain with next", "ordered processor list", "successor reference on handler"]
  - q: "Do handlers call a next() function or delegate to a successor to continue the chain?"
    weight: 3
    signals: ["next() callback", "call successor", "pass to next handler"]
  - q: "Can the chain be reconfigured (handlers added or removed) without modifying individual handler implementations?"
    weight: 2
    signals: ["dynamic chain construction", "pipeline.add handler", "middleware registration"]
  - q: "Is there a fallback or default handler at the end of the chain for unhandled requests?"
    weight: 1
    signals: ["terminal handler", "default fallback at chain end", "catch-all processor"]
threshold: 6

questions:
  - q: "Do classes implement the iterator protocol with __iter__/__next__, Iterator trait, or Symbol.iterator?"
    weight: 3
    signals: ["__iter__ and __next__", "impl Iterator", "Symbol.iterator"]
  - q: "Are elements produced lazily on demand rather than pre-computed into a full collection?"
    weight: 3
    signals: ["yield in generator", "next() returns one element", "lazy evaluation"]
  - q: "Are iterator combinators like map, filter, and chain used to compose traversals without materializing intermediate collections?"
    weight: 2
    signals: ["map/filter chain", "itertools.chain", "Stream.of().filter().map()"]
  - q: "Is resource cleanup handled on early termination of iteration?"
    weight: 1
    signals: ["finally in generator", "cleanup on StopIteration", "context manager for iterator"]
threshold: 6

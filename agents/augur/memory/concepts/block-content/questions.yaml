questions:
  - q: "Is content structured as typed blocks with a defined schema rather than raw HTML?"
    weight: 3
    signals: ["Block model with block_type field", "typed content units in JSON structure", "block schema definition"]
  - q: "Do blocks form a nested hierarchy with children arrays supporting composition?"
    weight: 3
    signals: ["children array on block nodes", "recursive block nesting", "tree structure in content model"]
  - q: "Is a structured editor framework (ProseMirror, Slate, Tiptap) used for editing?"
    weight: 2
    signals: ["ProseMirror or Slate imports", "Tiptap editor configuration", "Editor.js tool setup"]
  - q: "Can the block content be rendered to multiple output formats from a single source?"
    weight: 1
    signals: ["block-to-HTML renderer", "block-to-markdown converter", "cross-platform rendering from block JSON"]
threshold: 6

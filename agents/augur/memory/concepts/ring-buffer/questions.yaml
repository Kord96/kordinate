questions:
  - q: "Is there a fixed-size buffer with wrap-around indexing using modulo or bitwise arithmetic?"
    weight: 3
    signals: ["index % capacity for wrap-around", "head and tail pointers on fixed array", "power-of-two capacity with bitwise AND"]
  - q: "Does the buffer overwrite the oldest entry when full rather than resizing?"
    weight: 3
    signals: ["overwrite-oldest policy on full buffer", "no dynamic resize", "FIFO eviction on capacity limit"]
  - q: "Is the full vs empty state distinguishable when head equals tail?"
    weight: 2
    signals: ["separate count tracking occupancy", "sentinel distinguishing full from empty", "write pointer never equals read pointer ambiguity resolved"]
  - q: "Are read and write operations O(1) with pre-allocated storage?"
    weight: 1
    signals: ["pre-allocated array at construction", "constant-time enqueue and dequeue", "no heap allocation per operation"]
threshold: 6

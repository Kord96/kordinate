questions:
  - q: "Is there a ports/ and adapters/ directory structure with Port-suffixed interfaces and Adapter-suffixed implementations?"
    weight: 3
    signals: ["ports/ directory", "adapters/ directory", "RepositoryPort interface", "PostgresAdapter class"]
  - q: "Is the domain layer free of infrastructure imports like HTTP libraries, database drivers, or cloud SDKs?"
    weight: 3
    signals: ["no requests import in domain", "no boto3 in domain", "no DB driver in domain"]
  - q: "Are adapters wired to ports via dependency injection at application startup?"
    weight: 2
    signals: ["inject adapter at startup", "bind port to adapter", "DI container wiring"]
  - q: "Do tests use in-memory adapter implementations rather than mocking concrete infrastructure classes?"
    weight: 1
    signals: ["InMemoryRepository in tests", "fake adapter for testing", "test adapter implements port"]
threshold: 6

questions:
  - q: "Are components loaded via React.lazy, defineAsyncComponent, or dynamic import() in route definitions?"
    weight: 3
    signals: ["React.lazy(() => import(", "defineAsyncComponent", "loadChildren: () => import("]
  - q: "Are lazy-loaded components wrapped with Suspense or equivalent loading boundary with fallback UI?"
    weight: 3
    signals: ["Suspense fallback=", "loading boundary", "skeleton during load"]
  - q: "Does the build output show code splitting with separate chunks for lazily loaded modules?"
    weight: 2
    signals: ["webpackChunkName", "separate chunk files", "route-based splitting"]
  - q: "Are error boundaries in place to catch failed chunk loads from network errors or stale deployments?"
    weight: 1
    signals: ["ErrorBoundary around lazy", "chunk load error handling", "retry on failed import"]
threshold: 6

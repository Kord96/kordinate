# Augur skill bundles

These are stable operational bundles for Augur `/analyze`.

## Bundles

- `analyze-core-v1.md` — the stable analyze contract and execution-order layer

## Rule

Skill bundles should remain compact and cache-stable. They should define how `/analyze` operates, not preload the full semantic memory catalog.

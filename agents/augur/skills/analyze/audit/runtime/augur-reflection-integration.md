# Augur Reflection Integration

Use this reference when auditing how per-run reflections are captured and stored.

Check:

- raw reflection records are written durably
- reflection records carry model/provider/runtime metadata
- run manifests link to reflection records
- reflection storage can support later comparison and aggregation

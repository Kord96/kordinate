# Blast Radius Schema

Defines the deterministic manifest used to choose `skip`, `incremental`, or `full`.

Key fields:

- `mode`
- `tier`
- `reasons`
- `changed_files`
- `affected_components`
- `affected_flows`
- `affected_state`
- `affected_dependencies`
- `affected_concepts`
- `affected_fact_domains`

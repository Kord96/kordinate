# Atlas Schema Entry Point

`schema.md` is retained only as a short pointer for readers looking for the old path.

The active atlas contract is:
- [atlas-schema.md](/kord/workstation/home/project/kordinate/agents/augur/skills/analyze/atlas-schema.md)

Use that file for the current v4 structure, including:
- `flows` instead of `data_flows`
- attached `health` on components, flows, and external dependencies
- flow-level `business_metrics`

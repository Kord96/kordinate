# Project knowledge files sauron requires.
# On startup, /boot checks if these exist for the current project.
# Missing files are created from atlas output and monitoring.md standards.
# Project monitoring config at <project-repo>/monitoring/
# Dashboards at <project-repo>/monitoring/dashboards/
project_files:
  - metrics.md
  - vitals.md

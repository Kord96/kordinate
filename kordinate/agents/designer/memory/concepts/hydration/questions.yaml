questions:
  - q: "Does the application use framework SSR hydration APIs like hydrateRoot, createSSRApp, or TransferState?"
    weight: 3
    signals: ["hydrateRoot", "createSSRApp", "TransferState"]
  - q: "Is serialized state embedded in server-rendered HTML for the client to consume without re-fetching?"
    weight: 3
    signals: ["__NEXT_DATA__", "window.__NUXT__", "script type=application/json with state"]
  - q: "Are hydration mismatch warnings treated as bugs and investigated rather than globally suppressed?"
    weight: 2
    signals: ["hydration mismatch fix", "suppressHydrationWarning on specific element", "server/client render match"]
  - q: "Is there a two-phase rendering process where the server generates HTML and the client attaches event handlers?"
    weight: 1
    signals: ["SSR render then client hydrate", "dehydrate/rehydrate boundary", "HydrationBoundary component"]
threshold: 6

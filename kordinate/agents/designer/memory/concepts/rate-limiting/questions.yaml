questions:
  - q: "Are request counters tracked per client or IP with time window expiration?"
    weight: 3
    signals: ["token bucket implementation", "sliding window counter", "Redis INCR with EXPIRE for rate tracking"]
  - q: "Does the API return 429 status codes with rate limit headers?"
    weight: 3
    signals: ["429 Too Many Requests response", "X-RateLimit-Remaining header", "Retry-After header in responses"]
  - q: "Is rate limit state shared across replicas via an external store?"
    weight: 2
    signals: ["Redis-based rate counter", "distributed rate limit store", "shared counter across instances"]
  - q: "Are rate limits applied before expensive operations in the request pipeline?"
    weight: 1
    signals: ["rate limit middleware early in chain", "throttle check before handler", "limit applied at gateway layer"]
  - q: "Is the rate-limiting logic implemented in this codebase, or merely delegated to an external system?"
    weight: -2
    signals: ["rate limit delegated to external service", "control.rate_limit() forwarding to Celery/Redis", "rate limit proxy to another system"]
threshold: 6

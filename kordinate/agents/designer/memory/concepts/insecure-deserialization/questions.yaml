questions:
  - q: "Is pickle.loads(), eval(), or exec() called on data received from network requests, file uploads, or message queues?"
    weight: 3
    signals: ["pickle.loads on request body", "eval on user input", "exec on external data"]
  - q: "Is yaml.load() used without specifying SafeLoader on data from external sources?"
    weight: 3
    signals: ["yaml.load without SafeLoader", "yaml.load(data) no Loader", "unsafe YAML parsing"]
  - q: "Are deserialized objects used without schema validation to verify their structure and types?"
    weight: 2
    signals: ["no schema after deserialize", "trust deserialized object", "no type check post-parse"]
  - q: "Is Java ObjectInputStream.readObject() or PHP unserialize() called on user-supplied data?"
    weight: 2
    signals: ["readObject on network stream", "unserialize user data", "no ObjectInputFilter"]
  - q: "Does the codebase use pickle as a default serializer for job queues, message bodies, or cache values, even if a custom serializer can be configured?"
    weight: 2
    signals: ["pickle.dumps/pickle.loads as default serializer", "Serializer type alias defaulting to pickle", "disable_insecure_serializers function present"]
threshold: 6

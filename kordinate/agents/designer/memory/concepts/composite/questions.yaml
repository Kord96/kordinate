questions:
  - q: "Do leaf objects and container objects implement the same interface, allowing uniform treatment?"
    weight: 3
    signals: ["shared interface for leaf and composite", "uniform type for nodes", "polymorphic tree node"]
  - q: "Do composite nodes hold a children collection and delegate operations recursively to their children?"
    weight: 3
    signals: ["children list on composite", "recursive operation delegation", "iterate children and call same method"]
  - q: "Is there a recursive method (render, execute, calculate) that traverses the tree from root to leaves?"
    weight: 2
    signals: ["recursive traversal method", "tree walk calling same operation", "depth-first delegation"]
  - q: "Are add/remove child operations defined only on composite nodes, not on leaves?"
    weight: 1
    signals: ["child management on composite only", "leaf has no add/remove", "safe no-op on leaf"]
  - q: "Do compound elements (e.g., allOf/anyOf schemas, composite rules, nested validators) iterate over sub-elements and recursively apply the same validation or evaluation operation?"
    weight: 3
    signals: ["allOf iterating sub-schemas with same validator", "anyOf/oneOf combining sub-validator results", "compound rule delegating to child rules via same interface"]
threshold: 6

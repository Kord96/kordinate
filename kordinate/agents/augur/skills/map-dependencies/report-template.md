# Report Template

Write to `<project-repo>/.kord/agents/augur/memory/dependencies.md`. Create the directory if needed. Delegate to scribe if guard-md blocks.

This file is consumed by `/analyze` (step 3) to populate `external_dependencies`, `state`, and component `depends_on` fields in `architecture.yaml`. Use the concept vocabulary below so architect can match entries without translation.

## Concept Vocabulary

Use these exact values in the **Type** column of External Services and Infrastructure tables. Architect maps them directly to `concept` fields in `architecture.yaml`.

**For external_dependencies (Type column):** `http-api`, `message-broker`, `database`, `cache`, `object-store`, `dns`, `smtp`, `nfs`, `grpc`, `auth-provider`, `cdn`

**For state (inferred from Type + Technology):** `relational-db`, `document-store`, `embedded-olap`, `cache`, `object-store`, `message-broker`, `filesystem`, `in-memory`

When a service does not fit these terms, use the closest match and add detail in the Notes column.

```markdown
# <project> — Dependency Map

> Auto-generated by /augur:map-dependencies. Last run: <date>
> Language(s): <detected languages>
> Workspace: <monorepo layout if applicable, e.g., "pnpm monorepo: 5 packages, 2 shared libs" -- omit line for single-package projects>

## Module Graph

```
<ASCII dependency graph from step 9>
```

## Internal Modules

| Module | Imports | Imported by | Role |
|--------|---------|-------------|------|
| ... | ... | ... | hub / shared / leaf / standard (compound OK, e.g., "shared, hub") |

## External Services

| Type | Technology | Target | Used by | Config source |
|------|------------|--------|---------|---------------|
| database | PostgreSQL | `$DATABASE_URL` | models, store | env |
| cache | Redis | `$REDIS_HOST:6379` | cache | env |
| object-store | S3 | `$S3_BUCKET` | uploads | env |
| http-api | ... | `$PAYMENT_URL` | billing | config |

## Inter-service Dependencies

| Service | Discovered in | Pattern | Value |
|---------|---------------|---------|-------|
| ... | `.env` / `docker-compose.yml` / `config.yaml` | `*_URL` / `depends_on` / connection string | ... |

(Omit if no config-based service references found.)

## Infrastructure

| Resource | Kind | Namespace / Source | Notes |
|----------|------|-------------------|-------|
| ... | PVC / ConfigMap / Secret / Service / StatefulSet | k8s namespace | ... |
| ... | `aws_rds_instance` / `aws_sqs_queue` / `aws_s3_bucket` | Terraform module | ... |

(Or: "No k8s/IaC manifests found at standard paths.")

## Reverse Dependencies

| Project | Reference type | Files |
|---------|---------------|-------|
| ... | import / config / k8s manifest | ... |

(Omit this section if --reverse was not used.)

## Risks

- **Circular dependencies**: <list full cycle paths, or "None found">
- **Hub modules**: <module names imported by >50% of others -- coupling hotspots>
- **Hardcoded endpoints**: <file:line for each service URL literal not sourced from env/config>
- **Missing resilience**: <for each external service call lacking timeout, retry, or circuit breaker: file, service type, what is missing>
- **Unversioned external deps**: <external dependencies with no version pinning (unpinned pip install, `latest` image tags, missing lock files)>
```

## ASCII Graph Styles

**Box diagram** -- use for small graphs (under 6 modules):

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│   api    │────>│ service  │────>│  models  │
└──────────┘     └──────────┘     └──────────┘
     │                │                 ^
     │                v                 │
     │           ┌──────────┐           │
     │           │  cache   │           │
     │           └──────────┘           │
     v                                  │
┌──────────┐                            │
│   auth   │────────────────────────────┘
└──────────┘

External: [PostgreSQL] [Redis] [S3]
```

**Flat list** -- use for larger graphs (6+ modules):

```
api -> service, auth
service -> models, cache
auth -> models
cache -> (leaf)

External: PostgreSQL, Redis, S3
```

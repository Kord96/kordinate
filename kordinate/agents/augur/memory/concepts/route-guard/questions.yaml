questions:
  - q: "Are navigation guards or middleware checking auth state before rendering protected routes?"
    weight: 3
    signals: ["beforeEach with auth check", "CanActivate guard interface", "middleware.ts with NextResponse.redirect"]
  - q: "Do failed guards redirect to a login page with a return URL preserved?"
    weight: 3
    signals: ["redirect to /login with returnUrl param", "redirect on authentication failure", "return URL encoded in redirect"]
  - q: "Is guard logic centralized or composable rather than duplicated in every page component?"
    weight: 2
    signals: ["route-level guard configuration", "layout-level auth wrapper", "shared ProtectedRoute component"]
  - q: "Do guards handle loading or pending auth state without flashing protected content?"
    weight: 1
    signals: ["loading spinner while auth resolving", "guard blocks render until auth checked", "no flash of protected content before redirect"]
threshold: 6

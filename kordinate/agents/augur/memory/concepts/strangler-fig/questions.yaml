questions:
  - q: "Is there a routing layer splitting traffic between legacy and replacement systems by feature?"
    weight: 3
    signals: ["reverse proxy routing to old vs new service", "feature flag controlling which system handles request", "path-based routing between legacy and replacement"]
  - q: "Can each migrated feature be independently toggled back to legacy if issues arise?"
    weight: 3
    signals: ["per-feature rollback toggle", "migration flag reverts to legacy system", "independent cutover per endpoint"]
  - q: "Is data consistency maintained during dual-write periods with reconciliation?"
    weight: 2
    signals: ["dual-write to old and new stores", "reconciliation logic comparing systems", "event replay for data consistency"]
  - q: "Is migration progress measurable with a defined end state for legacy decommission?"
    weight: 1
    signals: ["percentage of traffic migrated tracked", "feature migration checklist", "plan for legacy system removal"]
threshold: 6

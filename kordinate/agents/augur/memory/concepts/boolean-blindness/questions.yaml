questions:
  - q: "Are there function calls that pass multiple boolean literals with no keyword names, making intent unreadable at the call site?"
    weight: 3
    signals: ["process(true, false, true)", "unnamed boolean args", "meaning invisible at call site"]
  - q: "Do function signatures accept two or more boolean parameters that could be accidentally swapped without type error?"
    weight: 3
    signals: ["multiple bool params", "swappable booleans", "compiler cannot catch arg swap"]
  - q: "Have boolean parameters accumulated over time as quick additions rather than being replaced with enums or config objects?"
    weight: 2
    signals: ["boolean parameter creep", "flag accumulation", "new bool added instead of refactoring"]
  - q: "Must developers jump to the function definition to understand what each boolean argument means at every call site?"
    weight: 2
    signals: ["requires cross-reference to understand", "boolean meaning not self-documenting", "context-dependent interpretation"]
threshold: 6

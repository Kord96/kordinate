questions:
  - q: "Are there SDL schema files defining Query, Mutation, or Subscription types with resolver functions mapping fields to data sources?"
    weight: 3
    signals: ["type Query {}", "type Mutation {}", "resolver function"]
  - q: "Does the API expose a single /graphql endpoint handling queries and mutations via selection sets?"
    weight: 3
    signals: ["/graphql endpoint", "selection set in query", "POST to single endpoint"]
  - q: "Are query depth or complexity limits enforced to prevent abusive queries?"
    weight: 2
    signals: ["depth limit", "complexity cost", "query validation rule"]
  - q: "Is the N+1 problem addressed with DataLoader or batching in resolvers?"
    weight: 1
    signals: ["DataLoader", "batch loader", "batched resolver"]
threshold: 6

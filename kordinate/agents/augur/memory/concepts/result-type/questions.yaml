questions:
  - q: "Are Result, Either, or similar types used to encode success and failure paths in return values?"
    weight: 3
    signals: ["Result<T, E> with Ok and Err", "Either<L, R> with Left and Right", "custom Result class with is_ok and is_err"]
  - q: "Are errors handled via match or fold on result types rather than try-catch for expected failures?"
    weight: 3
    signals: ["match on Ok/Err branches", "fold over success and failure", "no exceptions thrown for domain errors"]
  - q: "Are result types composed via map, flatMap, or and_then chains instead of nested conditionals?"
    weight: 2
    signals: ["chained .map().flatMap() on results", "railway-oriented .and_then() pipeline", "composable error transformation"]
  - q: "Are low-level errors mapped to domain-appropriate errors at boundary crossings?"
    weight: 1
    signals: ["error mapping at module boundary", "transform IO error to domain error", "typed error variants per layer"]
threshold: 6

questions:
  - q: "Is traffic split between a stable version and a new version using explicit percentage weights?"
    weight: 3
    signals: ["traffic weight configuration", "percentage-based routing", "canary vs stable split"]
  - q: "Are deployments promoted or rolled back based on metrics comparison (error rate, latency)?"
    weight: 3
    signals: ["metrics-driven promotion", "automated rollback on degradation", "canary analysis step"]
  - q: "Do deployment configurations define gradual traffic shifting in incremental steps?"
    weight: 2
    signals: ["stepWeight progression", "incremental traffic increase", "graduated rollout steps"]
  - q: "Is there a separate deployment or pod set labeled as canary running alongside the stable version?"
    weight: 1
    signals: ["canary-labeled deployment", "parallel canary pods", "canary subset definition"]
threshold: 6

questions:
  - q: "Are metrics defined using typed metric classes (Counter, Gauge, Histogram) and registered at startup?"
    weight: 3
    signals: ["Counter('http_requests_total')", "Histogram.new_float64()", "MustRegister()"]
  - q: "Is a /metrics endpoint exposed for Prometheus scraping?"
    weight: 3
    signals: ["/metrics HTTP endpoint", "prometheus exposition format", "metrics_handler route"]
  - q: "Do metric names follow a structured naming convention with namespace, subsystem, and unit?"
    weight: 2
    signals: ["http_requests_total", "request_duration_seconds", "namespace_subsystem_name_unit"]
  - q: "Are histogram buckets explicitly configured to match the expected distribution rather than using defaults?"
    weight: 2
    signals: ["buckets=[.01, .05, .1, .5, 1, 5]", "custom bucket boundaries", "tuned histogram"]
threshold: 6

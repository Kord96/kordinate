questions:
  - q: "Are there no-op implementations of interfaces (NullLogger, NoOpCache) used as defaults instead of null checks?"
    weight: 3
    signals: ["NullLogger class", "NoOpCache implements Cache", "DevNull writer"]
  - q: "Do call sites depend on the interface without checking whether they received a real or null implementation?"
    weight: 3
    signals: ["no if x is None guard", "polymorphic dispatch", "same interface real or null"]
  - q: "Are null objects stateless and safe to share as singletons?"
    weight: 2
    signals: ["singleton null object", "stateless no-op", "shared default instance"]
  - q: "Do null objects implement the full interface contract, not just a subset of methods?"
    weight: 2
    signals: ["all interface methods implemented", "no NotImplementedError", "complete contract"]
threshold: 6

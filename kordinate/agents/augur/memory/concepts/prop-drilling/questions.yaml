questions:
  - q: "Is the same prop passed through 5+ component layers where intermediate components only forward it without using it?"
    weight: 3
    signals: ["prop threaded through 5 levels", "intermediate component forwards without reading", "pass-through prop chain"]
  - q: "Do intermediate components have props in their interface that they never reference in their own render logic?"
    weight: 3
    signals: ["prop in signature but not in JSX", "spread forwarding unused props", "bloated prop interface"]
  - q: "Does adding a new prop to a leaf component require modifying every ancestor in the chain?"
    weight: 2
    signals: ["new prop requires 5 file changes", "ancestor chain updated for leaf change", "cascading prop additions"]
  - q: "Are there no Context, provide/inject, or state management solutions for data needed by deeply nested components?"
    weight: 2
    signals: ["no React.createContext", "no provide/inject", "no global store for shared data"]
threshold: 6

questions:
  - q: "Are blocking I/O calls like requests.get() or time.sleep() used inside async def functions?"
    weight: 3
    signals: ["requests.get() inside async def", "time.sleep() in coroutine", "urllib.request.urlopen() in async handler"]
  - q: "Are synchronous database drivers or file I/O used in async handlers without run_in_executor?"
    weight: 3
    signals: ["psycopg2 in async handler instead of asyncpg", "open() without aiofiles in async code", "synchronous ORM in async view"]
  - q: "Does the async server handle requests sequentially despite using an async framework?"
    weight: 2
    signals: ["throughput unchanged with concurrent requests", "event loop blocked on I/O wait", "sequential request handling in async app"]
  - q: "Are there asyncio warnings about long-running synchronous operations blocking the event loop?"
    weight: 1
    signals: ["asyncio slow callback warning", "Executing took X seconds log message", "event loop lag detected"]
threshold: 6

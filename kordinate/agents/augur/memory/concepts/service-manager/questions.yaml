questions:
  - q: "Are signal handlers registered for graceful shutdown with drain logic for in-flight requests?"
    weight: 3
    signals: ["SIGTERM handler with drain", "graceful shutdown flushing buffers", "terminationGracePeriodSeconds configured"]
  - q: "Are health endpoints exposed at /healthz and /readyz with corresponding K8s probes?"
    weight: 3
    signals: ["/healthz and /readyz endpoints", "livenessProbe and readinessProbe in pod spec", "health check reporting to orchestrator"]
  - q: "Does startup validate config and dependencies before marking the service as ready?"
    weight: 2
    signals: ["readiness delayed until dependencies connected", "startup validation before serving traffic", "ready signal only after initialization complete"]
  - q: "Are liveness and readiness probes distinct in behavior and purpose?"
    weight: 1
    signals: ["liveness checks process alive", "readiness checks dependencies available", "separate probe logic for live vs ready"]
threshold: 6

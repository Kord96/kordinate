questions:
  - q: "Are handlers stateless with event/context function signatures and no in-memory state between invocations?"
    weight: 3
    signals: ["handler(event, context) function signature", "stateless request processing", "no global state carried between invocations"]
  - q: "Is there FaaS infrastructure-as-code defining function deployments and triggers?"
    weight: 3
    signals: ["serverless.yml with function definitions", "SAM template.yaml with Lambda resources", "CDK Stack with Lambda constructs"]
  - q: "Are connections and heavy initializations placed outside the handler for warm invocation reuse?"
    weight: 2
    signals: ["database connection initialized at module level", "client created outside handler function", "connection pooling in global scope"]
  - q: "Are event-driven triggers handled idempotently for at-least-once delivery?"
    weight: 1
    signals: ["idempotency key on event processing", "deduplication for SQS or stream triggers", "safe to replay same event"]
threshold: 6

questions:
  - q: "Are there subscription and plan models with lifecycle states (trial, active, past_due, canceled)?"
    weight: 3
    signals: ["Subscription model with status field", "Plan model with pricing tiers", "subscription state transitions"]
  - q: "Is there integration with a payment provider for recurring charge processing?"
    weight: 3
    signals: ["Stripe or Paddle SDK usage", "payment intent creation", "recurring payment scheduling"]
  - q: "Does the system handle billing events via webhooks with idempotent processing?"
    weight: 2
    signals: ["webhook endpoint for payment events", "idempotent event handling", "webhook signature verification"]
  - q: "Is usage tracking or metered billing implemented for consumption-based pricing?"
    weight: 1
    signals: ["usage_meter or metered_billing", "consumption aggregation per billing period", "overage charge calculation"]
threshold: 6

questions:
  - q: "Do classes implement the iterator protocol with __iter__/__next__, __aiter__/__anext__, Iterator trait, Symbol.iterator, or Symbol.asyncIterator?"
    weight: 3
    signals: ["__iter__ and __next__", "__aiter__ and __anext__", "impl Iterator", "Symbol.iterator", "Symbol.asyncIterator", "AsyncGenerator", "async for"]
  - q: "Are elements produced lazily on demand rather than pre-computed into a full collection, including via async generators?"
    weight: 3
    signals: ["yield in generator", "async def with yield", "next() returns one element", "lazy evaluation", "async for consuming stream"]
  - q: "Are iterator combinators like map, filter, and chain used to compose traversals without materializing intermediate collections?"
    weight: 2
    signals: ["map/filter chain", "itertools.chain", "Stream.of().filter().map()"]
  - q: "Is resource cleanup handled on early termination of iteration?"
    weight: 1
    signals: ["finally in generator", "cleanup on StopIteration", "context manager for iterator"]
threshold: 6

questions:
  - q: "Is there a state machine with closed, open, and half-open states that governs calls to an external dependency?"
    weight: 3
    signals: ["circuit breaker state machine", "closed-open-half-open transitions", "state-based call gating"]
  - q: "Does a failure count threshold trigger the circuit to open, stopping calls to the failing dependency?"
    weight: 3
    signals: ["failure threshold", "trip on error count", "circuit opens after N failures"]
  - q: "Is there a half-open probe mechanism that allows limited test requests to determine if the dependency has recovered?"
    weight: 2
    signals: ["half-open probe", "limited test requests", "recovery check calls"]
  - q: "Is fallback behavior explicitly defined for when the circuit is open?"
    weight: 1
    signals: ["fallback on open circuit", "degraded response", "circuit-open handler"]
threshold: 6

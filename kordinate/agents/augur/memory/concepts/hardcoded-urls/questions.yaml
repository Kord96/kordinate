questions:
  - q: "Are URL string literals with hostnames or IP addresses present in production code paths rather than sourced from config?"
    weight: 3
    signals: ["http://localhost:8080 in source", "https://api.example.com literal", "IP address in code"]
  - q: "Are API base URLs defined as constants in application code rather than read from environment variables or config files?"
    weight: 3
    signals: ["BASE_URL = 'https://...'", "const API_HOST =", "no env var for endpoint"]
  - q: "Would deploying to a new environment require code changes to update service addresses?"
    weight: 2
    signals: ["no config override for URL", "hardcoded port number", "environment-specific code paths"]
  - q: "Do localhost or 127.0.0.1 references appear in non-test code without an environment-specific override?"
    weight: 1
    signals: ["localhost in production path", "127.0.0.1 without config fallback", "no service discovery"]
threshold: 6

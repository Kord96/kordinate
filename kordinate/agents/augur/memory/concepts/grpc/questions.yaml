questions:
  - q: "Are there .proto files with service and rpc declarations defining the API contract?"
    weight: 3
    signals: [".proto files", "service definition", "rpc method declaration"]
  - q: "Are generated stub files present for client and server code from proto definitions?"
    weight: 2
    signals: ["_pb2.py", "_grpc.pb.go", "generated stub"]
  - q: "Do RPC calls set deadlines or timeouts to prevent unbounded waits?"
    weight: 2
    signals: ["context.WithTimeout", "deadline", "grpc.timeout"]
  - q: "Are gRPC status codes used for error handling rather than generic error wrapping?"
    weight: 2
    signals: ["status.Errorf", "codes.NotFound", "grpc status code"]
threshold: 6

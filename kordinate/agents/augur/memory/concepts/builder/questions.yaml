questions:
  - q: "Is there a class with fluent setter methods (returning self/this) and a terminal build/create method that produces a different object?"
    weight: 3
    signals: ["fluent methods returning self", "terminal build() method", "builder produces product"]
  - q: "Does the build step validate that all required fields are set before constructing the final object?"
    weight: 2
    signals: ["validation in build()", "required field check", "prevents incomplete objects"]
  - q: "Are construction steps separated from the product's internal representation, allowing different representations from the same process?"
    weight: 3
    signals: ["construction separated from representation", "builder independent of product internals", "step-by-step assembly"]
  - q: "Can the builder be reused to create multiple instances without state from previous builds leaking into new ones?"
    weight: 2
    signals: ["builder reuse", "no state leakage between builds", "reset after build"]
threshold: 6

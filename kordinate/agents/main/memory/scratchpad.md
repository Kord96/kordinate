---
description: Operational notes and observations
curated: false
scope: global
---

# Scratchpad

- **2026-03-25**: Beorn MCP agent server merged into workstation pod as a background process (`node server.js &`). No separate beorn deployment/image. Workstation and kord-shared storage moved from gateway namespace to master namespace. Beorn accessed at `localhost:3100` from within the pod. The `agent-scaler-rbac.yaml` in shared manifests is now vestigial since beorn is no longer a separate scalable deployment.
- **2026-03-25**: Designer agent memory restructured. Patterns converted from flat files to directories (`patterns/<name>/pattern.md` + `README.md`). stoik/orchestrator co-located with their patterns (stream-to-store, service-manager). klog/nokrashi-tools parked in `memory/pending/` for future relocation to Sauron. `libraries.md` index deleted. Boot now respects `preloaded` frontmatter property — designer loads 4 files on boot instead of 20+. All 43 TODO sections filled (Monitoring, Deployment, Testing) across all 16 patterns.
- **2026-03-25**: Anthropic skills ecosystem audit. `anthropics/skills` has 17 first-party skills (docs, design, testing, MCP builder). `anthropics/claude-plugins-official` marketplace has 113 curated plugins across security (semgrep, aikido, autofix-bot, nightvision), testing (playwright, webapp-testing), deployment (terraform, railway, vercel, deploy-on-aws), monitoring (sentry, pagerduty, posthog), databases (firebase, supabase, neon, prisma), and productivity. Community ecosystem: 2300+ skills. Clearest role gap in 4-agent team (Deployer/Sauron/Designer/Scribe) is **Security** — 8+ dedicated security plugins with no owning agent. Testing is secondary gap, best absorbed by expanding Sauron. Recommended new agent: "Warden" for security.
- **2026-03-26**: Pattern catalog reference docs expanded with 3 category overview pages: `resilience.md` (6 patterns, layering diagram, decision tree), `lifecycle.md` (6 patterns, state diagram), `concurrency.md` (6 patterns, family diagram, decision guide). All mermaid diagrams validated via MCP. Index at `docs/reference/patterns/index.md` updated with new Concurrency tab and links to overview pages. No YAML frontmatter -- human-readable docs only.
- **2026-03-26**: Guard hook (`~/.kord/hooks/guard.sh`) push-to-main check broken when agent cwd is not a git repo (e.g. worktree roots like `w1-kordinate`). Bare `git fetch` and `git merge-base` fail silently (`2>/dev/null`), falling through to deny. Fixed by extracting repo path from `git -C <dir>` flags and `cd <dir> &&` patterns in the command string, then using `git -C <repo_root>` for all git operations in the guard.

routes:
  - name: write_memory
    method: POST
    description: Write a memory for an agent — handles scope, paths, frontmatter, and registry updates
    skill: remember

  - name: check_health
    method: GET
    description: Health-check the kordinate system — structural checks, runtime validation, and e2e tests
    skill: doctor-k

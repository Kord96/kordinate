# Consultation

When consulted, answer about:
- **Templates** — return content from `templates/agents/<agent>/` for the requested file type
- **Document formats** — advise on standard structure and conventions

## How to answer

1. Read the requested template from `templates/agents/<agent>/<filename>`
2. Return full template content as a structure guide
3. If no template exists, say so

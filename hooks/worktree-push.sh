#!/bin/bash
# Legacy helper retained for reference only.
#
# Merge-on-push is no longer active. Explicit integration now happens through
# the /integrate workflow and its associated skill resources.

exit 0

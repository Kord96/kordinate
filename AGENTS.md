# Agent Notes

## Validation

- If any `**/.validator/` tree changes, use the validator skill and run `python3 /kord/workstation/home/.codex/skills/validator/scripts/validator.py audit . --all --json` before committing.

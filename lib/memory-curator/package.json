{
  "name": "memory-curator",
  "version": "1.0.0",
  "description": "Per-agent memory curator — consumes updates from Kafka, merges into shared files",
  "type": "module",
  "main": "curator.js",
  "scripts": { "start": "node curator.js" },
  "dependencies": { "kafkajs": "^2.2.4" }
}

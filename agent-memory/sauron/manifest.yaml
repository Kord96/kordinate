# Project knowledge files sauron requires.
# On startup, /boot checks if these exist for the current project.
# Missing files trigger consultation with scribe for templates.
# Files are looked up in <project-repo>/.claude/agent-memory/sauron/
# Dashboards are discovered at <project-repo>/monitoring/dashboards/
project_files:
  - metrics.md
  - sentinel.md
